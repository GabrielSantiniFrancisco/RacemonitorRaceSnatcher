import os, sys
from datetime import datetime

current_date            = datetime.now().strftime('%Y%m%d')
script_name             = 'RaceTimeHandlerModule'

main_folder             = '/home/gabriel/projects/dev_race_monitor'
conf_folder             = os.path.join(main_folder, 'conf')
log_folder              = os.path.join(main_folder, 'logs')
log_file_path           = os.path.join(log_folder, current_date, f'{current_date}_{script_name}.log')

# Logging configuration
logging_config = {
    'enabled': True,
    'level': 'DEBUG',  # DEBUG, INFO, WARNING, ERROR, CRITICAL
    'log_to_file': True,
    'log_to_console': False,
    'log_file_path': log_file_path,
    'format': '%(asctime)s - [ %(transaction_id)s ] - %(name)s - %(levelname)s - %(message)s',
    'date_format': '%Y-%m-%d %H:%M:%S'
}